---
import { YouTube } from "astro-embed";

const { videoId } = Astro.props;
---

<YouTube
  id={videoId || "9oI-zvCBU4Y"}
  class="absolute inset-0 z-40 h-full w-full rounded-xl object-cover"
  params="autoplay=0&mute=1&rel=0"
  posterQuality="max"
/>
