---
import ImageComp from "@/components/utils/ImageComp.astro";
import partnerImg1 from "@/images/product-image-1.avif";
import { parentPort } from "worker_threads";
import PrimaryCTA from "@components/ui/buttons/PrimaryCTA.astro";

// Define props from Astro
const { title, subTitle, partners, btnURL, btnTitle } = Astro.props;

interface Partner {
  icon: any;
  name?: string;
  href?: string;
  imagePath?: string;
  imageAlt?: string;
  btnURL?: string;
  btnTitle?: string;
  subTitle?: string;
  description?: string;
}

// Define TypeScript interface for props
interface Props {
  title: string;
  subTitle?: string;
  partners: Partner[];
  btnURL?: string;
  btnTitle?: string;
}
---

<section
  class="mx-auto max-w-[85rem] bg-white px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
>
  <!-- Title and description -->
  <div class="mx-auto mb-6 w-full space-y-1 text-center sm:w-1/2 lg:w-1/3">
    <h2
      class="text-2xl leading-tight font-bold text-balance text-neutral-800 uppercase sm:text-3xl dark:text-neutral-200"
    >
      <Fragment set:html={title} />
    </h2>
    {
      subTitle && (
        <p class="leading-tight text-pretty text-neutral-600 dark:text-neutral-400">
          {subTitle}
        </p>
      )
    }
  </div>
  <div
    class="flex max-w-7xl flex-col flex-wrap items-center justify-around gap-y-2 rounded-2xl p-10 sm:flex-row sm:gap-x-12 sm:gap-y-0 lg:gap-x-24 lg:gap-y-12 dark:bg-white/[.075]"
  >
    <!-- Clients Group SVGs -->
    {
      partners.map((partner) => (
        <a
          href={partner.href}
          target="_blank"
          rel="noopener noreferrer"
          class="bg-picton-blue-500/40 group hover:border-picton-blue-400 flex min-h-48 max-w-1/2 min-w-48 items-center justify-center gap-2 rounded-2xl border-2 px-8 py-4 duration-500 ease-out hover:bg-white"
        >
          <ImageComp
            imagePath={partner.imagePath}
            imageAlt={partner.imageAlt}
            quality={60}
            styles="max-w-48"
            loading="lazy"
          />
          <div class="hidden flex-col items-start justify-center gap-2 px-2 text-neutral-600 transition duration-700 ease-out group-hover:flex dark:text-neutral-200">
            <h3 class="font-semibold text-neutral-800 uppercase">
              {partner.name}
            </h3>
            <p class="text-picton-blue-600 text-sm font-bold">
              {partner.subTitle}
            </p>
            <p class="text-xs">{partner.description}</p>
          </div>
        </a>
      ))
    }
  </div>

  <div
    class="mx-auto mb-6 w-full space-y-1 py-12 text-center sm:w-1/2 lg:w-1/3"
  >
    {btnTitle && <PrimaryCTA title={btnTitle} url={btnURL} />}
  </div>
</section>
